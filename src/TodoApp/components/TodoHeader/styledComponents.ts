import tw, { styled } from 'twin.macro'

import { Typo36Grey20RalewayBold } from '../../../Common/typos'

export const HeaderContainer = styled.div`
   ${tw`
        flex justify-center
    `}
`

export const TitleText = styled(Typo36Grey20RalewayBold)``
